{% extends '::fronted.html.twig' %}
{% block title %}Tipos de documentos de identificación{% endblock %}
{% block encabezado %}Tipos de documentos de identificación{% endblock %}

{% block contenedor %}  

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Tipo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tipodocidentificacion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.tipo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tipodocidentificacion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tipodocidentificacion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tipodocidentificacion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
