{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PagoCuotaCredito</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Valoringresado</th>
                <td>{{ entity.valorIngresado }}</td>
            </tr>
            <tr>
                <th>Fechadepago</th>
                <td>{{ entity.fechaDePago|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('pagocuotacredito') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pagocuotacredito_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
