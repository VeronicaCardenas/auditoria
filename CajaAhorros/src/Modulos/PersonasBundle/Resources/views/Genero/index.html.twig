{% extends '::fronted.html.twig' %}
{% block title %}Generos{% endblock %}
{% block encabezado %}Generos{% endblock %}

{% block contenedor %}  


   

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Genero</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('genero_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.genero }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('genero_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('genero_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('genero_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
