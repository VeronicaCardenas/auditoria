{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MetodoAmortizacionAhorro</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Metodo</th>
                <td>{{ entity.metodo }}</td>
            </tr>
            <tr>
                <th>Nombremetodo</th>
                <td>{{ entity.nombreMetodo }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.descripcion }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ entity.activo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('metodoamortizacionahorro') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('metodoamortizacionahorro_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
